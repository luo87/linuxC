#include <stdio.h>
#include <string.h>

int main(){
	char peoples[] = "ABCDE";
	int i;
	for(i = 0; i < strlen(peoples); i++){
		char people = peoples[i];
		int level;
		for(level = 1; level <= 5; level++){
			//bool A = ();
			printf("df");
		}
	}
}
